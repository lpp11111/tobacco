# 烟草训练系统 - 详细文件说明

## 根目录文件

### train_large.py
**作用**: 大尺度模型训练脚本 - 圈地和株数统计

**详细说明**:
- 使用 YOLOv8m-seg 分割模型
- 自动适配 4通道(RGB+NDVI)输入
- 优化的训练参数适合大尺度场景
- 结果保存到 `large/large_train/`

**关键参数**:
- 模型: yolov8m-seg.pt
- Batch size: 8
- Epochs: 100
- Patience: 30

**使用方法**:
```bash
python train_large.py
```

---

### train_medium.py
**作用**: 中尺度模型训练脚本 - 病害检测

**详细说明**:
- 使用 YOLOv8m-seg 分割模型
- 自动适配 4通道输入
- 平衡的训练参数
- 结果保存到 `medium/medium_train/`

**关键参数**:
- 模型: yolov8m-seg.pt
- Batch size: 4
- Epochs: 120
- Patience: 35

**使用方法**:
```bash
python train_medium.py
```

---

### train_small.py
**作用**: 小尺度模型训练脚本 - 精细病害分级

**详细说明**:
- 使用 YOLOv8m-seg 分割模型
- 自动适配 4通道输入
- 更强的数据增强（mixup, shear）
- 结果保存到 `small/small_train/`

**关键参数**:
- 模型: yolov8m-seg.pt
- Batch size: 8
- Epochs: 100
- Patience: 25

**使用方法**:
```bash
python train_small.py
```

---

### README.md
**作用**: 项目总览和快速开始指南

**详细说明**:
- 项目概述
- 三个尺度的特点介绍
- 快速开始使用方法
- 目录结构说明
- 训练配置对比表

---

## 训练结果目录

### large/large_train/
**作用**: 大尺度训练结果目录（训练后自动生成）

**内容说明**: 包含分割相关文件（MaskF1_curve.png, MaskPR_curve.png等）

#### large/large_train/weights/best.pt
**作用**: 最佳模型权重文件

**详细说明**:
- 验证集上表现最好的模型
- 推荐用于推理和部署
- 根据验证集mAP自动选择

#### large/large_train/weights/last.pt
**作用**: 最新模型权重文件

**详细说明**:
- 训练最后一个epoch的权重
- 可用于断点续训

#### large/large_train/results.csv
**作用**: 训练过程指标记录

**详细说明**:
- CSV格式，每个epoch一行
- 包含训练损失、验证损失、mAP、精度、召回率等
- 可用于绘制训练曲线

#### large/large_train/results.png
**作用**: YOLO自动生成的训练曲线图

#### large/large_train/args.yaml
**作用**: 训练参数配置记录

**详细说明**:
- YAML格式
- 记录本次训练的所有参数
- 用于实验复现

#### large/large_train/F1_curve.png, PR_curve.png等
**作用**: 各种评估指标曲线（检测和分割）

#### large/large_train/MaskF1_curve.png
**作用**: 分割Mask的F1分数曲线

#### large/large_train/MaskPR_curve.png
**作用**: 分割Mask的Precision-Recall曲线

#### large/large_train/MaskP_curve.png
**作用**: 分割Mask的Precision曲线

#### large/large_train/MaskR_curve.png
**作用**: 分割Mask的Recall曲线

#### large/large_train/confusion_matrix.png
**作用**: 混淆矩阵

**详细说明**:
- 展示分类错误情况
- 行是真实类别，列是预测类别
- 对角线是正确分类

#### large/large_train/labels.jpg
**作用**: 训练集标签统计

#### large/large_train/train_batch*.jpg
**作用**: 训练批次样本可视化

#### large/large_train/val_batch*.jpg
**作用**: 验证批次样本和预测结果

---

### medium/medium_train/
**作用**: 中尺度训练结果目录

**内容说明**: 与 large/large_train/ 结构相同，包含分割相关文件。

---

### small/small_train/
**作用**: 小尺度训练结果目录

**内容说明**: 与 medium/medium_train/ 结构相同，包含分割相关文件。

---

## 文件重要性排序

### 最核心文件（必须保留）
1. **train_large.py** - 大尺度训练脚本
2. **train_medium.py** - 中尺度训练脚本
3. **train_small.py** - 小尺度训练脚本
4. **large/large_train/weights/best.pt** - 大尺度最佳模型
5. **medium/medium_train/weights/best.pt** - 中尺度最佳模型
6. **small/small_train/weights/best.pt** - 小尺度最佳模型

### 重要文件（建议保留）
7. **README.md** - 项目文档
8. **FILE_GUIDE.md** - 本文件
9. **large/large_train/results.csv** - 训练记录
10. **medium/medium_train/results.csv**
11. **small/small_train/results.csv**
12. **large/large_train/results.png** - 训练曲线
13. **medium/medium_train/results.png**
14. **small/small_train/results.png**

### 参考文件（可选保留）
15. 各种曲线图（F1_curve.png, PR_curve.png等）
16. 混淆矩阵
17. 训练/验证批次样本图
18. 其他日志文件

---

## 常见问题

### Q: 如何只训练其中一个尺度？
A: 分别运行对应的脚本：
- 大尺度: `python train_large.py`
- 中尺度: `python train_medium.py`
- 小尺度: `python train_small.py`

### Q: 训练中断了怎么办？
A: 可以直接重新运行对应的脚本，或者使用 last.pt 作为预训练权重继续训练。

### Q: 显存不足怎么办？
A: 编辑对应训练脚本，减小 batch_size 参数。

### Q: 如何调整训练参数？
A: 直接编辑对应的训练脚本，修改 model.train() 的参数。

### Q: 如何使用训练好的模型？
A:
```python
from ultralytics import YOLO
model = YOLO("train/large/large_train/weights/best.pt")
results = model.predict("path/to/image.png")
```

### Q: 为什么三个尺度都使用同样的模型？
A: 三个尺度统一使用YOLOv8m-seg分割模型，但根据各自特点调整训练参数（batch size、epochs、数据增强等）。

---

## 数据来源
原始数据集位于：
- 大尺度: `../large/`
- 中尺度: `../medium/`
- 小尺度: `../small/`

每个数据集都是标准的 YOLO 格式，包含 train/ 和 valid/ 划分。
